/*
 * TyperShowdownPage Messages
 *
 * This contains all the text for the TyperShowdownPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TyperShowdownPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TyperShowdownPage container!',
  },
});
